torch>=2.1.0
transformers>=4.45.0
Pillow>=9.0
pyyaml>=6.0
pandas>=1.5
pyarrow>=11.0
tqdm>=4.65
# Optional: for LoRA support
# peft>=0.7.0
# Optional: for Gaussian blur in grounding loss
# torchvision>=0.16.0
